<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:context="http://www.springframework.org/schema/context"
	xmlns:tx="http://www.springframework.org/schema/tx" xmlns:mvc="http://www.springframework.org/schema/mvc"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="
	http://www.springframework.org/schema/beans
	http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
	http://www.springframework.org/schema/context
	http://www.springframework.org/schema/context/spring-context-3.0.xsd
	http://www.springframework.org/schema/tx
	http://www.springframework.org/schema/tx/spring-tx-3.0.xsd 
	http://www.springframework.org/schema/mvc   
    http://www.springframework.org/schema/mvc/spring-mvc-3.0.xsd
    http://www.springframework.org/schema/aop
	http://www.springframework.org/schema/aop/spring-aop-3.0.xsd">

	<!--对web包中的所有类进行扫描 -->
	<context:component-scan base-package="com.springMVC" />

	<!-- 国际化 -->
	<bean id="messageSource"
		class="org.springframework.context.support.ResourceBundleMessageSource">
		<property name="basenames" value="messages"></property>
		<property name="useCodeAsDefaultMessage" value="true"></property>
	</bean>
	<mvc:interceptors>
		<bean class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor" />
	</mvc:interceptors>
	<bean id="localeResolver"
		class="org.springframework.web.servlet.i18n.SessionLocaleResolver" />

	<!-- 配置数据源数据 -->
	<context:property-placeholder location="classpath:db.properties" />
	
	<!--
	<bean id="dataSource"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${database.driver}" />
		<property name="url" value="${database.url}" />
		<property name="username" value="${database.user}" />
		<property name="password" value="${database.password}" />
	</bean>

	<bean id="dataSourceNavis"
		class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="${navis.driver}" />
		<property name="url" value="${navis.url}" />
		<property name="username" value="${navis.user}" />
		<property name="password" value="${navis.password}" />
	</bean>
	-->
	
	
	<bean id="dataSource" class = "com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
		<property name="driverClass" value="${database.driver}" />
		<property name="jdbcUrl" value="${database.url}" />
		<property name="user" value="${database.user}" />
		<property name="password" value="${database.password}" /> 

		<!-- connection pool -->
		<property name="maxIdleTime" value="${vmt.c3p0.maxIdleTime}" />
		<property name="minPoolSize" value="${vmt.c3p0.min_size}" />
		<property name="maxPoolSize" value="${vmt.c3p0.max_size}" />
		<property name="initialPoolSize" value="${vmt.c3p0.initialPoolSize}" />
		<property name="checkoutTimeout" value="${vmt.c3p0.checkoutTimeout}" />
		<property name="unreturnedConnectionTimeout" value="${vmt.c3p0.unreturnedConnectionTimeout}" />
 	</bean>
 	
	<aop:config>
	    <aop:advisor 
	        pointcut="execution(java.sql.Connection javax.sql.DataSource.getConnection(..))" 
	        advice-ref="getDatabaseConnectionInterceptor"/>
	</aop:config>
	
	<bean id="getDatabaseConnectionInterceptor" class="org.springframework.data.jdbc.aop.ConnectionInterceptor">
	    <property name="connectionPreparer" ref="connectionPreparer"/>
	</bean>

	<bean id="connectionPreparer" class="com.accenture.vmt.ClientIdentifierConnectionPreparer"/>
	
 	<!--
	<bean id="dataSourceNavis" class="com.mchange.v2.c3p0.ComboPooledDataSource" destroy-method="close">
		<property name="driverClass" value="${navis.driver}" />
		<property name="jdbcUrl" value="${navis.url}" />
		<property name="user" value="${navis.user}" />
		<property name="password" value="${navis.password}" />
		
       	<property name="maxIdleTime" value="${n4.c3p0.maxIdleTime}" />
       	<property name="minPoolSize" value="${n4.c3p0.min_size}" />
       	<property name="maxPoolSize" value="${n4.c3p0.max_size}" />
       	<property name="initialPoolSize" value="${n4.c3p0.initialPoolSize}" />
	</bean>
	-->
	


	<!--配置seesionFactory工厂 -->
	<bean id="sessionFactory"
		class="org.springframework.orm.hibernate3.annotation.AnnotationSessionFactoryBean">
		<property name="dataSource">
			<ref bean="dataSource" />
		</property>

		<property name="annotatedClasses">
			<list>
				<value>com.springMVC.entity.User</value>
				<value>com.springMVC.entity.ShowLog</value>
				<value>com.springMVC.entity.ColSet</value>
				<value>com.springMVC.entity.CellMatrix</value>
				<value>com.springMVC.entity.Vessel</value>
				<value>com.springMVC.entity.VesselCol</value>
				<value>com.springMVC.entity.VesselRefuel</value>
				<value>com.springMVC.entity.OperationLog</value>
			</list>
		</property>

		<property name="hibernateProperties">
			<props>
				<prop key="hibernate.dialect">${hibernate.dialect}</prop>
				<prop key="hibernate.show_sql">${hibernate.show_sql}</prop>
			</props>
		</property>
	</bean>

	<!--对模型视图名称的解析， -->
	<bean id="jspViewResolver"
		class="org.springframework.web.servlet.view.InternalResourceViewResolver">
		<property name="viewClass"
			value="org.springframework.web.servlet.view.JstlView" />
		<property name="prefix" value="/WEB-INF/jsp/" />
		<property name="suffix" value=".jsp" />
	</bean>

	<!-- 配置 事务 -->
	<bean id="transactionManager"
		class="org.springframework.orm.hibernate3.HibernateTransactionManager">
		<property name="sessionFactory" ref="sessionFactory" />
	</bean>

	<!-- 使用annotation定义事务 -->
	<tx:annotation-driven transaction-manager="transactionManager" />

	<mvc:interceptors>
		<mvc:interceptor>
			<mvc:mapping path="/user/*" />
			<bean class="com.springMVC.filter.SecurityInterceptor">
				<property name="excludedUrls">
					<list>
						<value>login</value>
						<value>index</value>
						<value>logout</value>
						<value>changeLan</value>
					</list>
				</property>
			</bean>
		</mvc:interceptor>
	</mvc:interceptors>

	<bean id="hibernateTemplate" class="org.springframework.orm.hibernate3.HibernateTemplate">
		<property name="sessionFactory" ref="sessionFactory"></property>
	</bean>

	<bean id="jdbcTemplate" class="org.springframework.jdbc.core.JdbcTemplate">
		<!--
		<property name="dataSource" ref="dataSourceNavis"></property>
		-->
		<property name="dataSource" ref="dataSource"></property>
	</bean>


</beans>