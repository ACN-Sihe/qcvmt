用户提到"代码使用当前逆向工程连接的Git代码"，我需要先找到逆向工程的代码仓库。让我探索一下。
找到了逆向工程代码在 `/workspace/canvas_opencode/qcvmt-22/`。让我读取所有关键源文件以确保骨架代码准确反映原始业务逻辑。
继续读取 DAO 层、Controller 层和核心业务逻辑代码。
继续读取剩余的工具类和配置文件。
现在我已经完整阅读了逆向工程的所有源代码。让我更新执行计划，确保完全基于原始代码的实际字段名、表名和业务逻辑。
## 执行计划（基于逆向工程代码分析）
已完成对 `qcvmt-22` 逆向工程仓库全部 **33 个 Java 源文件**、**pom.xml**、**配置文件** 的深度阅读。
### 原始代码 → Spring Boot 3 关键映射
| 维度 | 原始代码 (qcvmt-22) | 新项目 (qcvmt) |
|------|----------------|
| 框架 | Spring MVC 3.0.1 + HibernateTemplate | Spring Boot 3.5.16 + Spring Data JPA |
| 包名 | `com.springMVC.*`, `com.accenture.vmt.*` | `com.mtl.qcvmt.*` |
| 构建 | Maven WAR (Java 1.7) | Gradle Kotlin DSL (Java 17) |
| 认证 | Session + 手写 SecurityInterceptor | Keycloak OIDC + Spring Security |
| 数据源 | C3P0 + 单 JdbcTemplate (Oracle) | HikariCP + JPA(MySQL) + JdbcTemplate(Oracle只读) |
| 表名 | T_USER, T_Vessel, T_VESSELCOL 等 | t_user, t_vessel, t_vessel_col 等 |
| 主键 | SEQUENCE(Oracle) | IDENTITY(MySQL AUTO_INCREMENT) |
| 时间字段 | String (`yyyyMMddHHmmss`) | LocalDateTime |
| 响应 | JSP + XML (Busihandler) | REST JSON (ApiResponse) |
### 原始实体字段精确映射（已验证）
| 原始实体 | 原始列名 | 新实体保留列名 | 类型变更 |
|----------|---------------|----------|
| User | userid, QCID, NAME, PASSWORD, ROLE, PARENT, CREATETIME | id→userid, keycloak_id(新增), qcid, name, role, parent, createtime | String→LocalDateTime, 移除password |
| Vessel | vmid, vesselid, deck_hold, bay, rowstart, rowend, tierstart, tierend | 保持原始列名 | 不变 |
| VesselCol | vcid, vesselid, deck_hold, bay, rowstart, rowend, tierstart, tierend | 保持原始列名 | 不变 |
| VesselRefuel | vrid, vesselid, is_refuel | 保持原始列名 | 不变 |
| ColSet | colsetid, COLOR, BOXCASE | 保持原始列名 | 不变 |
| CellMatrix | matrixid, cmtype, cmrow, cmtier, active | 保持原始列名 | 不变 |
| ShowLog | userlogid, USERID, USERNAME, QCID, LOGINTIME, OPERATION | 保持原始列名 | String→LocalDateTime |
| OperationLog | OPERLOGID, USERNAME, FUNCTION, ACTIONTYPE, VALUECHANGE, TIME | 保持原始列名 | Date→LocalDateTime |
### N4 Oracle SQL 迁移（CellDaoImpl → N4 Service 层）
| 原始方法 | 涉及 N4 表 | 迁移目标 |
|-----------|----------|
| `getQorder()` → `getDischargeOrder()` / `getLoadOrder()` | inv_wq, inv_wi, inv_unit_yrd_visit, inv_unit_fcy_visit, inv_unit, xps_craneshift, xps_pointofwork, ref_equipment, inv_goods, argo_carrier_visit | N4WorkQueueService |
| `getSequenceList()` | 同上 | N4WorkQueueService |
| `checkSequenceList()` | 同上 | N4WorkQueueService |
| `getROBList()` / `getROBListByBay()` / `getROBListByBayNew()` | inv_unit_fcy_visit, inv_unit, argo_carrier_visit | N4ContainerQueryService |
| `getCellMatrixFromnN4()` | argo_carrier_visit, vsl_vsl_visit_details, vsl_vessels, t_vessel | N4VesselQueryService |
| `queryQcId()` | xps_pointofwork, argo_yard, argo_facility | N4FacilityQueryService |
| `queryFacilityByQcId()` | argo_facility, argo_yard, xps_pointofwork | N4FacilityQueryService |
| `getN4VesselNameById()` | vsl_vessels | N4VesselQueryService |
| `getHazardList()` | ref_hazardous_material, inv_unit | N4ContainerQueryService |
### Controller 映射（原始 → REST）
| 原始 Controller 方法 | 新 REST 端点 |
|---------------------|-------------|
| `UserControl.login()` | 移除（Keycloak 接管） |
| `UserControl.listAllUser()` | `GET /api/users` |
| `UserControl.addUser()` / `save()` | `POST /api/users` |
| `UserControl.modUser()` / `updateUser()` | `PUT /api/users/{id}` |
| `UserControl.delUser()` | `DELETE /api/users/{id}` |
| `UserControl.showLog()` | `GET /api/users/{id}/logs` |
| `UserControl.exportLogs()` | `GET /api/export/logs` |
| `UserControl.importVessel()` | `POST /api/import/vessel` |
| `CellControl.busiQuery()` | `GET /api/terminal/query` |
| `CellControl.getAllColSet()` | `GET /api/color-sets` |
| `CellControl.saveColSet()` | `POST /api/color-sets` |
| `CellControl.updateColSet()` | `PUT /api/color-sets/{id}` |
| `CellControl.delColSet()` | `DELETE /api/color-sets/{id}` |
| `CellControl.getVessel()` | `GET /api/vessels` |
| `CellControl.saveVessel()` | `POST /api/vessels` |
| `CellControl.updateVessel()` | `PUT /api/vessels/{id}` |
| `CellControl.delVessel()` | `DELETE /api/vessels/{id}` |
| `CellControl.getVesselCol()` | `GET /api/vessel-colors` |
| `CellControl.saveVesselCol()` | `POST /api/vessel-colors` |
| `CellControl.delVesselCol()` | `DELETE /api/vessel-colors/{id}` |
| `CellControl.getVesselRefuel()` | `GET /api/vessel-refuels` |
| `CellControl.updateVesselRefuelStatus()` | `POST /api/vessel-refuels` |
| `CellControl.delVesselRefuel()` | `DELETE /api/vessel-refuels/{id}` |
| `CellControl.setBaySize()` | `GET /api/bay-config` |
| `CellControl.updateBay()` | `PUT /api/bay-config` |
### 文件生成清单（共 ~65 个文件）
1. **Gradle 构建** (3): build.gradle.kts, settings.gradle.kts, gradle wrapper
2. **启动类** (1): QcvmtApplication.java
3. **config/** (7): MysqlDataSourceConfig, N4OracleDataSourceConfig, SecurityConfig, KeycloakConfig, CorsConfig, WebConfig, I18nConfig
4. **entity/** (10): User, Vessel, VesselCol, VesselRefuel, ColSet, CellMatrix, ShowLog, OperationLog, BaySize, SequenceVO
5. **repository/** (8): UserRepository, VesselRepository, VesselColRepository, VesselRefuelRepository, ColSetRepository, CellMatrixRepository, ShowLogRepository, OperationLogRepository
6. **n4/** (2): N4QueryRepository, N4TableConstants
7. **security/** (2): JwtAuthConverter, SecurityContextHelper
8. **service/local/** (7): UserService, VesselColorService, VesselRefuelService, ColorSetService, CellMatrixService, OperationLogService
9. **service/keycloak/** (2): KeycloakUserSyncService, KeycloakRoleMappingService
10. **service/n4/** (4): N4VesselQueryService, N4WorkQueueService, N4FacilityQueryService
11. **dto/request/** (8): CreateUserRequest, UpdateUserRequest, CreateVesselRequest, UpdateVesselRequest, CreateColSetRequest, UpdateColSetRequest, SaveVesselColRequest, SaveVesselRefuelRequest
12. **dto/response/** (8): UserResponse, VesselResponse, ColSetResponse, VesselColResponse, VesselRefuelResponse, TerminalView, WorkQueueResult, PageResponse
13. **dto/** (1): ApiResponse
14. **controller/** (9): UserController, VesselController, ColorSetController, VesselColorController, VesselRefuelController, BayConfigController, TerminalController, OperationLogController, ImportExportController
15. **exception/** (3): GlobalExceptionHandler, BusinessException, N4ConnectionException
16. **util/** (3): WebUtil, ImportHandler, ExportHandler
17. **resources/** (8): application.yml, application-dev/sit/uat/prod.yml, messages_en/zh_CN/zh_TW.properties
18. **SQL** (1): schema.sql
19. **.gitignore** (1)
是否确认开始执行？
---
## 参考项目文件
以下文档按加载顺序作为生成依据：
2. **启动类** (1): QcvmtApplication.java
17. **resources/** (8): application.yml, application-dev/sit/uat/prod.yml, messages_en/zh_CN/zh_TW.properties
<!-- tdlc:md-preprocess:125 -->